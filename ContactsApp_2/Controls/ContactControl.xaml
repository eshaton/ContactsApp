<UserControl x:Class="ContactsApp_2.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactsApp_2.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="300">
    <Grid Background="LightGray">
        <StackPanel Margin="5">
            <TextBlock x:Name="nameTextBlock" Text="Name Lastname"
                       FontSize="20"
                       FontWeight="Bold"/>
            <TextBlock x:Name="emailTextBlock" Text="example@gmail.com"
                       FontSize="15"
                       Foreground="DodgerBlue"/>
            <TextBlock x:Name="phoneTextBlock" Text="++385 216 7654"
                       FontSize="15"
                       FontStyle="Italic"/>
        </StackPanel>     
    </Grid>
</UserControl>
